spring:
  datasource:
    url: jdbc:h2:file:./db/filmorate
    driverClassName: org.h2.Driver
    username: sa
    password: password
  sql:
    init:
      mode: never

#lombok:
#  copyableAnnotations: += org.springframework.beans.factory.annotation.Qualifier